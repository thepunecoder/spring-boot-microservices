server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username : sa
    password : ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform : org.hibernate.dialect.H2Dialect
    hibernate :
      ddl-auto : update
      show-sql : true

build:
  version: 1.0.0

accounts:
  message: "Welcome to ThePuneCoder accounts related local APIs"
  contactDetails:
      name: "Devarsh W - Developer"
      email: "devarsh@thepunecoder.com"
  onCallSupport:
      - (555) 555-1234
      - (555) 523-1345